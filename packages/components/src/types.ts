/** Section label color role (maps to `brand.*` Tailwind tokens in the host app). */
export type SectionLabelVariant = 'emphasis' | 'on-dark' | 'muted';

/** Metric grid color role for value/label text. */
export type MetricGridVariant = 'light' | 'dark';
